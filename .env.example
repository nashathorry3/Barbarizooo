# Copy to .env and fill in. Used by docker-compose.yml.
# cp .env.example .env

# --- Domains (point both A-records at the VPS IP first) ---
DOMAIN=barbarizoo.de
API_DOMAIN=api.barbarizoo.de

# --- Database ---
DB_USER=barbarizoo
DB_PASSWORD=change-me-to-a-strong-password

# --- Security (generate: openssl rand -base64 48) ---
JWT_SECRET=change-me-to-a-long-random-48char-plus-secret

# --- Optional: Stripe (defaults to simulated payments if unset) ---
PAYMENTS_PROVIDER=simulated
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# --- Optional: Google Sign-In (defaults to mock if unset) ---
GOOGLE_AUTH_MODE=mock
GOOGLE_CLIENT_ID=

# --- Optional: Email reminders (defaults to simulated/log if unset) ---
# Set provider=email and fill the SMTP details from your provider (Brevo, Postmark, Gmail…).
NOTIFICATIONS_PROVIDER=simulated
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=no-reply@barbarizoo.de
MAIL_FROM_NAME=Barbarizoo
